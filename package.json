{
  "name": "raha-cloud-pos",
  "version": "1.0.0",
  "description": "The RAHA Cloud-Based Restaurant POS System",
  "main": "server.js",
  "scripts": {
    "start": "pwd && ls -la && test -d public && ls -la public || echo 'PUBLIC FOLDER MISSING!' && node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1",
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
